# =====================================================
# APPLICATION IDENTITY
# =====================================================
spring.application.name=rules-engine-service

# =====================================================
# SERVER PORT
# =====================================================
server.port=8082

# =====================================================
# KAFKA CONFIGURATION
# =====================================================
spring.kafka.bootstrap-servers=localhost:9092

# CONSUMER CONFIG
# group-id ? identifies this service as a consumer group.
# All instances of rules-engine-service share this group ID
# so Kafka delivers each message to only one instance,
# preventing duplicate processing.
spring.kafka.consumer.group-id=rules-engine-group
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer

# trusted-packages ? security setting that tells the
# JsonDeserializer which Java packages it is allowed
# to deserialize. We trust our own com.sentinel package.
spring.kafka.consumer.properties.spring.json.trusted.packages=com.sentinel.*

# auto-offset-reset ? if this service starts and there
# are already messages in Kafka it hasn't seen,
# "earliest" means start from the beginning.
spring.kafka.consumer.auto-offset-reset=earliest

# =====================================================
# ACTUATOR
# =====================================================
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always


# =====================================================
# KEEP APPLICATION ALIVE
# This service has no web server ? it runs as a
# background Kafka consumer. Without this, Spring Boot
# starts and immediately shuts down because it thinks
# there is nothing to do. This keeps the JVM running
# so the service stays alive waiting for Kafka messages.
# =====================================================
spring.main.keep-alive=true



# =====================================================
# REDIS
# Points to Redis running in Docker Compose.
# Used exclusively for velocity checks.
# =====================================================
spring.data.redis.host=localhost
spring.data.redis.port=6379

# =====================================================
# KAFKA TOPICS
# =====================================================
sentinel.kafka.topics.transactions-raw=transactions.raw
sentinel.kafka.topics.fraud-scored=fraud.scored

# =====================================================
# RULES ENGINE CONFIG
# Business thresholds ? externalised so they can
# change without code changes.
# =====================================================

# Max transactions per account within the velocity window
sentinel.rules.velocity.max-transactions=5

# Velocity check time window in seconds
sentinel.rules.velocity.window-seconds=60

# Score added when velocity rule fires
sentinel.rules.velocity.score-contribution=35

# Score added when amount threshold rule fires
sentinel.rules.amount.score-contribution=25

# Score added when off-hours rule fires
sentinel.rules.off-hours.score-contribution=15

# Score added when new device rule fires
sentinel.rules.device.score-contribution=20